import React from 'react';
import ReactDOM from 'react-dom/client';
import ApuestasSimuladas from './src/ApuestasSimuladas.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ApuestasSimuladas />
  </React.StrictMode>,
);